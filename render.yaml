services:
  - type: web
    name: webrtc-video-chat
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_STUN_SERVER
        value: stun:stun.l.google.com:19302
